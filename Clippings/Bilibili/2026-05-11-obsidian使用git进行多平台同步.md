---
title: "obsidian使用git进行多平台同步"
url: "https://www.bilibili.com/video/BV1qCh9zrEKq/?spm_id_from=333.1007.0.0&vd_source=2a7d090dc9c46dd40318fd20366d6d22"
bvid: "BV1qCh9zrEKq"
cid: "32023185748"
author: "嗷呜_嗷嗷嗷_"
upload_date: "2025-08-29"
subtitle_lang: "中文"
created: "2026-05-11"
tags: ["clippings", "bilibili"]
---

<iframe src="https://player.bilibili.com/player.html?aid=115110911744683&bvid=BV1qCh9zrEKq&cid=32023185748&page=1&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

该方案手机端同步偏慢，我已改用gitsync进行同步。
视频所需文件，访问下文链接获取：https://hugo.15390974.xyz/docs/obsidian-git%E5%90%8C%E6%AD%A5%E7%AC%94%E8%AE%B0/     
补充:操作顺序很重要，麻烦细看视频，如果已经传过手机，再更新gitignore,需按上文链接红字4，清理缓存，再手动同步一次解决。  另:为避免冲突，使用过程中请等待手机同步完成，再去电脑端修改，不要一端改了还没同步，又去另一端修改
不想折腾的可以付费，召唤本人远程帮你搞定。

## 字幕

`00:00` AMAZAN是一款非常好用的笔记软件
`00:03` 但是它的同步一直是一个大问题呃
`00:07` 网上一般都推荐利用第三方工具
`00:09` 比如说呃坚果云进行同步
`00:12` 但它需要始终保持软件在后台运行
`00:17` 我觉得不太好
`00:19` 然后git的话又说配置起来比较麻烦
`00:22` 但我实测还是比较简单的
`00:25` 因此有了今天这个视频
`00:27` 先来看一下同步效果啊
`00:29` 我在这里输入额DR
`00:33` 这是第二次工作
`00:36` 然后的话可以等几分钟让它自动同步
`00:39` 也可以的话
`00:40` 点击右上角这个按钮
`00:42` 让它自动同步好额
`00:44` 这边劝举变成了零
`00:46` 其实就是已经同步完成了呃
`00:48` 然后的话我们打开手机看一下
`00:51` 你在打开软件的时候
`00:53` 它会自动拉取一次更新
`00:54` 但是你想嘛他从网上下载下来
`00:57` 所以要等一会的
`00:58` 再然后的话手机上由于它通过插件同步的话
`01:02` 速度会比电脑上慢一点
`01:04` 这里看一下
`01:12` 同理手动给它同步一下嗯
`01:16` 一般需要等他这个转完发现了改变再上传
`01:20` 稳妥一点
`01:23` 好然后点击上传
`01:25` 等他这边转完
`01:26` 基本上就算成功了
`01:29` 额这边劝举还是一的话
`01:31` 证明没有成功
`01:32` 再稍微等一下
`01:36` 也不用一直盯着啊
`01:37` 其实可以就是说点完同步就放后台
`01:40` 这已经同步完成了
`01:41` 同步完成之后的话啊
`01:43` 电脑这里直接过来了
`01:46` 再来分析一下同步实现的原理
`01:48` 方便理解后面的操作
`01:50` 首先的话无论什么软件进行一个同步的话
`01:53` 都是将你的本地文件上传到他们公司的云端
`01:57` 服务器
`01:58` 那当你呃要在别的机器上看的话
`02:01` 再从云端机器下载到你的另一台本地设备
`02:04` 那呃我们这里也是同样的道理啊
`02:07` 就是说我们可以选择自建一个git服务器
`02:10` 也可以利用免费的呃get it服务器
`02:13` 比如说github gt呃
`02:15` 进行一个同步呃
`02:17` 别看他们免费啊
`02:18` 其实的话安全性比百度云
`02:21` 或者说坚果云都是差不多的
`02:24` 而且上传限制可能还要小一点
`02:27` 那当然如果说你想要绝对的安全
`02:31` 然后同时也不希望有什么上传限制
`02:34` 想传多大传多大的话
`02:35` 那你就只能说自建一个git服务器
`02:38` 那既然服自建服务器嘛
`02:40` 可能就得额外花钱了
`02:42` 我这里以这个GITEE为例
`02:44` 展示一下安装及配置的全过程
`02:48` 总体流程其实不多啊
`02:50` 就像我这里画的
`02:51` 首先安装一下git和UZI
`02:53` 之后的话
`02:54` 去GITTE网站上配置一下
`02:57` 再配置一下object的插件呃
`03:00` 然后基本上就算可以同步了吧
`03:03` 那手呃手电脑上配置好了
`03:05` 手机端就更简单
`03:06` 直接的话把文件夹传输过去之后
`03:09` 用ABBZ点打开文件夹就可以了
`03:14` 下面来讲一下软件安装啊
`03:15` 已经安装的可以直接跳过
`03:17` 然后的话这两个网站分别是
`03:20` obsidian和git的官方发布页
`03:23` 点进去
`03:25` 点进去的话
`03:26` 手机安装的话就是下载这个点APK文件
`03:29` 然后电脑上就是这个点EXE文件
`03:32` 那下载完成之后双击安装
`03:34` 这个应该不用再演示了啊
`03:36` 然后手机这边需要说明一下
`03:39` 就是说因为这不属于应用商店的软件
`03:42` 现在很多手机不允许安装
`03:44` 你可以通过断网来跳过它的安全验证
`03:47` 然后安装
`03:49` 就是说你下载完成之后先断网
`03:51` 再点击安装软件的话
`03:54` 这是官方发布页
`03:55` 有很多很多人用
`03:57` 所以的话肯定是安全的
`04:01` 至于git的话
`04:03` 只需要在电脑上安装呃
`04:05` 我这是win系统的这个下载链接
`04:07` 同样是点进去
`04:08` 点进去之后点击这个黄色的字体进行一个下载
`04:14` 下载完成之后的话
`04:15` 其实就是我桌面上这样一个安装包
`04:19` 同理是双击安装呃
`04:21` 按照它默认的直接一路next就行
`04:24` 不需要进行额外配置
`04:26` 现在都不需要去配置那个PATH路径了
`04:29` 如果是老版本
`04:30` 可能需要新版本
`04:31` 不用
`04:33` 软件安装完成之后
`04:35` 就去配置一下我们的远程服务器GITEE
`04:39` 那首先打开它的网站
`04:43` 呃默认打开是这个样子的
`04:45` 然后注册一下
`04:47` 当然有账号就可以不用注册
`04:49` 名字的话
`04:50` 这里用英文
`04:51` 而且的话你得去想一个别人没有用过的这样的
`04:55` 因为它的每个名称都是独立的
`04:57` 那你可以用你的手机号或者QQ号
`04:59` 再加上几个字母就可以了
`05:02` 然后的话输入手机号以及你要使用的密码
`05:06` 这个密码要记一下
`05:07` 因为你在后面配置的时候会用到
`05:10` 包括这个名字也要也最好记一下
`05:15` 注册完成之后登录
`05:23` 登录的话
`05:24` 首先点击呃
`05:26` 把鼠标移到右上角的头像这里
`05:29` 然后往下滑找到账号设置
`05:31` 有时候他可能就是设置两个字都一样的
`05:34` 点进去点进去往下滑
`05:37` 找到SSH攻略
`05:39` 然后怎样生成攻略
`05:44` 复制这行代码
`05:48` 然后回到桌面
`05:49` 鼠标放在空白的地方
`05:51` 按住键盘shift别松
`05:54` 然后按鼠标右键找到在此处打开power窗口呃
`05:59` 可能文字不一样
`06:00` 意思一样就行
`06:02` 这个时候会弹出一个黑框框
`06:04` 然后鼠标右键就会把刚才复制的代码
`06:08` 粘贴进来回车
`06:10` 然后这个时候继续回车
`06:12` 继续回车好可以了
`06:15` 再回到教程这边往下翻
`06:18` 找到c AC at这段代码
`06:21` 同样给它复制
`06:26` 鼠标右键粘贴到黑框框
`06:28` 也就是终端回车
`06:30` 这个时候会出现一串密文
`06:33` 从最开始一直复制到呃
`06:36` 可以看一下是第二个空格
`06:38` 这里空格不用复制好
`06:41` 右键右键就直接复制了啊
`06:43` 选中之后右键就直接复制了
`06:46` 然后回到刚才攻略这个页面
`06:49` 把刚才的这段密文粘贴进来
`06:53` 然后前面标题可以任意取一个名字之后
`06:58` 点击确定
`07:00` 输入账号密码
`07:04` 验证
`07:06` 然后你在上面就可以看到已经添加了这个蜜月
`07:12` 这一步是必须要有的
`07:14` 否则的话待会输入密码
`07:16` 可能无法通过创建蜜月成功之后
`07:21` 再点击右上角这个加号
`07:24` 点击新建仓库
`07:28` 然后给仓库取一个名字
`07:30` 那我们这里笔记的话
`07:31` NOTE我已经有了一个
`07:33` 所以再加一个一之后
`07:35` 注意把这个开源点成私有
`07:38` 如果说你开源的话
`07:40` 别人也能看到你这个仓库私有的话
`07:43` 就仅有自己可见之后点击确定
`07:47` 好现在就可以了
`07:49` 进入这个页面
`07:50` 再点击初始化率的me
`07:52` 这一步一定要点
`07:53` 要不然待会儿同步可能会失败
`07:56` 再点再点击这个克隆下载
`08:00` 复制这个HTTPSS的链接
`08:03` 回到电脑的桌面
`08:05` 鼠标移动到空白处
`08:07` 按住键盘shift键不要放
`08:09` 再按住鼠标右键
`08:11` 同样打开POWERSHELL窗口
`08:14` 然后输入hit c l o n e克隆
`08:19` 再空格
`08:20` 鼠标右键直接粘贴
`08:25` 然后再按回车
`08:27` 这个时候会让我们输入账号
`08:29` 账号就是EGITE这边刚才注册时候的账号
`08:35` 回车再继续输入密码
`08:38` 在终端输入密码是隐藏的
`08:40` 所以的话不用担心
`08:42` 输入完成之后按回车就行
`08:49` 然后的话像这样看到一个百分百就算是成功了
`08:53` 你会在你的桌面找到一个
`08:55` 和我们刚才创建的仓库同名的文件夹
`09:01` 这个文件夹
`09:02` 就是我们之后所有笔记所存放的文件夹
`09:05` 你可以把它移动到你想要存放的位置
`09:09` 而且的话只需要备份
`09:11` 它就能够将我们所有的笔记进行一个备份转移
`09:16` 然后就可以打开我们的笔记软件和不Z
`09:19` 点双击打开
`09:21` 然后刚安装的话
`09:23` 他可能是个英文界面
`09:24` 你在这里选中简体中文
`09:26` 切换成中文
`09:27` 然后再点击打开本地仓库
`09:31` 找到我们刚才的这个仓库
`09:36` 选择文件夹它就自动打开了
`09:38` 打开之后我们首先需要安装一下插件额
`09:42` 点击画面左下角偏中间吧
`09:46` 这个齿轮图标设置
`09:48` 然后第三方插件点击关闭安全模式
`09:53` 再点击浏览git搜索gi it
`09:56` 第一个就是点一下
`09:59` 再在弹出的界面点击安装
`10:01` 稍微等一会儿安装完成
`10:04` 再点一下启用
`10:05` 然后呢点击选项进入设置页面呃
`10:10` 前两个按钮的话是控制它隔多少分钟自动同步
`10:14` 你可以以分钟为单位
`10:16` 可以设置一分钟
`10:17` 也可以设置更多
`10:18` 由于它在手机上可能稍微有点慢的话
`10:21` 这个作用其实不太好
`10:23` 可以关掉
`10:24` 然后的话往下翻
`10:26` 找到这个pull on startup
`10:28` 这个就是说当我们打开这个
`10:30` obsidian软件的时候
`10:31` 进行一次同步
`10:32` 这个可以打开
`10:33` 再然后的话往下翻
`10:36` 在这个提交作者这里输入一下
`10:39` 随便输个名字就行
`10:41` 这个是你在提交时候用的作者名
`10:46` 然后包括邮箱也可以随便填
`10:48` 当然最好是一个自己能记住的
`10:51` 有辨识度的之后
`10:53` 点击这个叉其实就保存了啊
`10:56` 然后我们可以新建一个笔记
`10:58` 看一下教学
`11:01` 很多测试随便输入点文字
`11:06` 然后在他的左边工具栏这里找到这个图标
`11:10` 点一下
`11:12` 然后呢
`11:13` 这右上角的这个向上的箭头就是同步的意思
`11:16` 点一下好
`11:18` 那你看到这个charge是零的时候
`11:20` 其实就已经给它上传到了我们的服务器
`11:23` gi it e1这边
`11:26` 呃
`11:29` 你看这边多出来一个文件
`11:32` 下面就是最重要的两步
`11:35` 用于防止手机和电脑同步产生的冲突
`11:39` 首先点开我们的笔记文件夹
`11:41` 然后需要在此处打开终端呃
`11:45` 像之前一样
`11:46` 鼠标键盘按住shift不要松开
`11:49` 再按鼠标右键打开power窗口
`11:53` 那此时的窗口路径其实就是你当前的文件夹了
`11:56` 这个时候输入代码
`12:03` 这段代码的作用是禁止检查文件权限改动
`12:08` 如果说不执行的话
`12:10` 即使你没有对文件进行任何改动
`12:13` 也会被检测成改动
`12:15` 容易造成冲突
`12:18` 回车他没有报错
`12:19` 就算执行成功
`12:22` 再之后还是回到笔记文件夹这里打开它
`12:26` 然后的话注意一下有没有显示这个点
`12:30` 什么什么后缀
`12:31` 如果显示了就进行下一步
`12:32` 没有显示的话
`12:33` 就点击查看
`12:35` 然后在显示这边
`12:37` 勾选文件扩展名以及隐藏的项目
`12:41` 选上之后就能够看见后缀名了
`12:45` 当你能看见后缀名之后
`12:48` 在文件夹里新建一个文本文档
`12:52` 然后呢将这段代码给它粘贴进去
`12:56` 代码文件之后我会放在评论区
`13:02` 然后保存它
`13:05` 保存之后再将它的名字改为
`13:10` 点注意是英文的点
`13:13` H i t i g n o r e
`13:18` 然后是
`13:22` 这个文件的作用是忽略一些缓存文件
`13:26` 因为在电脑上和手机上的缓存不同
`13:29` 同样会造成一些同步上的冲突
`13:35` 那电脑端这边的配置就完成啦
`13:38` 下面讲一下手机端如何配置
`13:42` 手机端的话
`13:43` 你首先需要将我们的笔记文件夹拷贝过去
`13:46` 那无论是说插上数据线传输过去
`13:49` 还是给它压缩一下
`13:51` 再通过QQ传过去都行
`13:53` 但是注意一点
`13:54` 你需要知道你传输过去之后
`13:57` 你的文件具体是在哪个位置
`14:00` 我这里直接从电脑上传
`14:06` 为了方便寻找
`14:08` 我直接放在这个360的文件夹里
`14:23` 文件传输完成之后
`14:25` 就可以去手机上来打开obsidian这个软件
`14:32` 我这里由于已经把自己的笔记同步过来了啊
`14:35` 所以是这个画面
`14:36` 如果是刚安装的话
`14:37` 应该会让你选择打开某个文件夹
`14:41` 那我在这边再选一下
`14:42` 也可以是这个页面
`14:44` 然后的话我们选择第三个打开本地文件夹
`14:48` 然后呢就去找一下
`14:52` 找一下我们刚才传过来的文件
`14:57` 刚才我是放在了360里
`14:59` 然后note1允许访问允许
`15:04` 然后再信任并启用呃
`15:08` 这个时候输入一下我们在gi it e那边的账号名
`15:24` 再输入一下密码
`15:27` 输入密码
`15:37` 回车
`15:40` 然后我们返回到文件的编辑页面
`15:44` 点击右下角
`15:46` 如果右下角不是当前的样子的话
`15:48` 就在把输入法点出来
`15:51` 再将它缩小
`15:53` 然后点击两次右上角的三个点
`15:56` 它就变成了这个样子
`16:00` 然后打开这个open git
`16:03` 这个时候我们在编辑页面右滑的话
`16:06` 就可以从这个窗口进入同步界面了
`16:10` 然后我们需要手动同步的话
`16:12` 点击一下向上的箭头就行
`16:14` 重复一遍呃
`16:15` 编辑完成之后的话
`16:17` 如果想要手动同步往右滑一下
`16:20` 然后点击向上的箭头就可以了
`16:24` 可以测试一下啊
`16:26` 啧
`16:33` 编辑完成之后往右滑
`16:35` 点击同步
`16:38` 呃手机端这边同步是会比电脑端要慢的
`16:43` 然后的话来电脑端这边看一下
`16:46` 同样拉取一下更新
`16:49` 嗯好这就同步过来了
`16:51` 和手机端一样
`16:53` 然后讲一下我为什么选择使用git
`16:56` 进行obsidian的多端同步
`16:58` 首先的话get it
`17:00` 作为当前阶段最强大的版本管理工具
`17:03` 只需要稍微花点时间即可实现
`17:07` 将你的文件回退到任意一次同步历史
`17:12` 看我现在看
`17:15` 我这里其实就是每一次按时间同步的历史
`17:19` 当然其实也可以自己在同步的时候
`17:22` 设定一下记录
`17:23` 方便回退
`17:26` 而且相比于别的同步方案
`17:28` 比如说坚果云
`17:30` 它不需要你在手机或者电脑安装
`17:33` 额外安装别的软件
`17:36` 如果看的人多
`17:38` 我会再做一个回退历史以及解决冲突的教程
`17:44` 再见