---
type: concept
course: 控制系统工程Ⅱ
chapter: [3]
first_seen: 2025-10-22
prerequisites: [稳态误差, 系统型别]
related: [开环增益, 终值定理]
contrasts: [动态误差]
---

# 静态误差系数

## 定义

静态误差系数是衡量系统跟踪不同输入信号时稳态精度的指标，包括静态位置误差系数 K_p、静态速度误差系数 K_v 和静态加速度误差系数 K_a。它们分别对应系统跟踪阶跃、斜坡和抛物线输入时的稳态精度。

## 直觉理解

好比不同运动项目的"精准度评分"：K_p 是"定点投篮"的精准度，K_v 是"定点射门"的精准度，K_a 是"定点投飞镖"的精准度。每个系数衡量系统在不同运动模式下的跟踪精度。

## 前置概念

- 稳态误差：系统稳定后，期望输出与实际输出的差值
- 系统型别：开环传递函数中积分环节的个数
- 终值定理：e_ss = lim_{s→0} s·E(s)，用于由 E(s) 求稳态误差

## 推导到 / 关联到

- 稳态误差：静态误差系数用于计算不同输入信号下的稳态误差
- 开环增益：静态误差系数与开环增益直接相关
- 系统设计：通过调整静态误差系数来改善系统稳态精度

## 易混概念

- 动态误差：系统暂态过程中的误差，随时间变化；静态误差系数用于计算稳态误差
- 开环增益：开环传递函数中的比例系数，影响静态误差系数大小

## 典型例子

- 静态位置误差系数 K_p：K_p = lim_{s→0} G(s)，用于计算阶跃输入下的稳态误差
- 静态速度误差系数 K_v：K_v = lim_{s→0} s·G(s)，用于计算斜坡输入下的稳态误差
- 静态加速度误差系数 K_a：K_a = lim_{s→0} s²·G(s)，用于计算抛物线输入下的稳态误差